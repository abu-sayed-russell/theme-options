jQuery(document).ready(function ($) {  $('#frmfield').on('submit', function (e) {    e.preventDefault();    $('.has-error').removeClass('has-error');    var form = $(this),      title = form.find('#title').val(),      description = form.find('#description').val(),      get_id = form.find('#get_id').val(),      field_type = form.find('#field_type').val(),      menu_id = form.find('#menu_id').val(),      status = form.find('#status').val(),      ajaxurl = form.data('url');    if (title === '') {      $('#title').parent().addClass('has-error');      return;    }    if (description === '') {      $('#description').parent().addClass('has-error');      return;    }    if (get_id === '') {      $('#get_id').parent().addClass('has-error');      return;    }    if (field_type === '') {      $('#field_type').parent().addClass('has-error');      return;    }    if (menu_id === '') {      $('#menu_id').parent().addClass('has-error');      return;    }    if (status === '') {      $('#status').parent().addClass('has-error');      return;    }    var save_data = "action=getweb_option_save_field&param=save_field&" + jQuery("#frmfield").serialize();    $.ajax({      url: ajaxurl,      type: 'post',      data: save_data,      success: function (response) {        var data = jQuery.parseJSON(response);        var html = '';        var success_popup = $('#of-popup-save');        var fail_popup = $('#of-popup-fail');        if (data.status == 1) {          success_popup.fadeIn();          html = '<span>' + data.msg + '</span>';          success_popup.html(html);          window.setTimeout(function () {            success_popup.fadeOut();          }, 1500);          jQuery("#frmfield")[0].reset();        } else if (data.error == 2) {          fail_popup.fadeIn();          html = '<span>Something wrong, try again!</span>';          fail_popup.html(html);          window.setTimeout(function () {            fail_popup.fadeOut();          }, 1500);        } else if (data.status == 2) {          fail_popup.fadeIn();          fail_popup.addClass('of-popup-fail');          html = data.error;          fail_popup.html(html);          window.setTimeout(function () {            fail_popup.fadeOut();          }, 1500);        }      }    });  });});jQuery(document).ready(function ($) {  $('#frmeditgetwebfield').on('submit', function (e) {    e.preventDefault();    $('.has-error').removeClass('has-error');    var form = $(this),      title = form.find('#title').val(),      description = form.find('#description').val(),      get_id = form.find('#get_id').val(),      field_type = form.find('#field_type').val(),      menu_id = form.find('#menu_id').val(),      status = form.find('#status').val(),      ajaxurl = form.data('url');    if (title === '') {      $('#title').parent().addClass('has-error');      return;    }    if (description === '') {      $('#description').parent().addClass('has-error');      return;    }    if (get_id === '') {      $('#get_id').parent().addClass('has-error');      return;    }    if (field_type === '') {      $('#field_type').parent().addClass('has-error');      return;    }    if (menu_id === '') {      $('#menu_id').parent().addClass('has-error');      return;    }    if (status === '') {      $('#status').parent().addClass('has-error');      return;    }    var update_data = "action=getweb_option_save_field&param=edit_field&" + jQuery("#frmeditgetwebfield").serialize();    $.ajax({      url: ajaxurl,      type: 'post',      data: update_data,      success: function (response) {        var data = jQuery.parseJSON(response);        var html = '';        var success_popup = $('#of-popup-save');        var fail_popup = $('#of-popup-fail');        if (data.status == 1) {          success_popup.fadeIn();          html = '<span>' + data.msg + '</span>';          success_popup.html(html);          window.setTimeout(function () {            success_popup.fadeOut();          }, 1500);        } else if (data.error == 2) {          fail_popup.fadeIn();          html = '<span>Something wrong, try again!</span>';          fail_popup.html(html);          window.setTimeout(function () {            fail_popup.fadeOut();          }, 1500);        } else if (data.status == 2) {          fail_popup.fadeIn();          fail_popup.addClass('of-popup-fail');          html = data.error;          fail_popup.html(html);          window.setTimeout(function () {            fail_popup.fadeOut();          }, 1500);        }      }    });  });});jQuery(document).on("click", ".delete_field", function () {  if (confirm('Are you sure you want to delete this?')) {    var getweb_delete_id = jQuery(this).attr("data-id");    var delete_data = "action=getweb_option_save_field&param=delete_field&id="+getweb_delete_id;    $.ajax({      url: ajaxurl,      type: 'post',      data: delete_data,      success: function (response) {        var data = jQuery.parseJSON(response);        var html = '';        var success_popup = $('#of-popup-save');        var fail_popup = $('#of-popup-fail');        if (data.status == 1) {          success_popup.fadeIn();          html = '<span>' + data.msg + '</span>';          success_popup.html(html);          window.setTimeout(function () {            success_popup.fadeOut();            location.reload();          }, 1000);        } else if (data.error == 2) {          fail_popup.fadeIn();          html = '<span>Something wrong, try again!</span>';          fail_popup.html(html);          window.setTimeout(function () {            fail_popup.fadeOut();            location.reload();          }, 1000);        } else if (data.status == 2) {          fail_popup.fadeIn();          fail_popup.addClass('of-popup-fail');          html = data.error;          fail_popup.html(html);          window.setTimeout(function () {            fail_popup.fadeOut();            location.reload();          }, 1000);        }      }    });  }});