jQuery(document).ready(function ($) {  $('#frmgetweb').on('submit', function (e) {    e.preventDefault();    $('.has-error').removeClass('has-error');    var form = $(this),      name = form.find('#name').val(),      status = form.find('#status').val(),      extra_class = form.find('#extra_class').val(),      ajaxurl = form.data('url');    if (name === '') {      $('#name').parent().addClass('has-error');      return;    }    if (status === '') {      $('#status').parent().addClass('has-error');      return;    }    var save_data = "action=getweb_option_save_menu&param=save_menu&" + jQuery("#frmgetweb").serialize();    $.ajax({      url: ajaxurl,      type: 'post',      data: save_data,      success: function (response) {        var data = jQuery.parseJSON(response);        var html = '';        var success_popup = $('#of-popup-save');        var fail_popup = $('#of-popup-fail');        if (data.status == 1) {          success_popup.fadeIn();          html = '<span>' + data.msg + '</span>';          success_popup.html(html);          window.setTimeout(function () {            success_popup.fadeOut();          }, 1500);          jQuery("#frmgetweb")[0].reset();        } else if (data.error == 2) {          fail_popup.fadeIn();          html = '<span>Something wrong, try again!</span>';          fail_popup.html(html);          window.setTimeout(function () {            fail_popup.fadeOut();          }, 1500);        } else if (data.status == 2) {          fail_popup.fadeIn();          fail_popup.addClass('of-popup-fail');          html = data.error;          fail_popup.html(html);          window.setTimeout(function () {            fail_popup.fadeOut();          }, 1500);        }      }    });  });});jQuery(document).ready(function ($) {  $('#frmeditgetweb').on('submit', function (e) {    e.preventDefault();    $('.has-error').removeClass('has-error');    var form = $(this),      name = form.find('#name').val(),      status = form.find('#status').val(),      extra_class = form.find('#extra_class').val(),      ajaxurl = form.data('url');    if (name === '') {      $('#name').parent().addClass('has-error');      return;    }    if (status === '') {      $('#status').parent().addClass('has-error');      return;    }    var update_data = "action=getweb_option_save_menu&param=edit_menu&" + jQuery("#frmeditgetweb").serialize();    $.ajax({      url: ajaxurl,      type: 'post',      data: update_data,      success: function (response) {        var data = jQuery.parseJSON(response);        var html = '';        var success_popup = $('#of-popup-save');        var fail_popup = $('#of-popup-fail');        if (data.status == 1) {          success_popup.fadeIn();          html = '<span>' + data.msg + '</span>';          success_popup.html(html);          window.setTimeout(function () {            success_popup.fadeOut();          }, 1500);        } else if (data.error == 2) {          fail_popup.fadeIn();          html = '<span>Something wrong, try again!</span>';          fail_popup.html(html);          window.setTimeout(function () {            fail_popup.fadeOut();          }, 1500);        } else if (data.status == 2) {          fail_popup.fadeIn();          fail_popup.addClass('of-popup-fail');          html = data.error;          fail_popup.html(html);          window.setTimeout(function () {            fail_popup.fadeOut();          }, 1500);        }      }    });  });});jQuery(document).on("click", ".deletegetweb", function () {  if (confirm('Are you sure you want to delete this?')) {    var getweb_delete_id = jQuery(this).attr("data-id");    var delete_data = "action=getweb_option_save_menu&param=delete_menu&id=" + getweb_delete_id;    $.ajax({      url: ajaxurl,      type: 'post',      data: delete_data,      success: function (response) {        var data = jQuery.parseJSON(response);        var html = '';        var success_popup = $('#of-popup-save');        var fail_popup = $('#of-popup-fail');        if (data.status == 1) {          success_popup.fadeIn();          html = '<span>' + data.msg + '</span>';          success_popup.html(html);          //$("#theme_option_table").load(" #theme_option_table > *");          window.setTimeout(function () {            success_popup.fadeOut();            location.reload();          }, 1000);        } else if (data.error == 2) {          fail_popup.fadeIn();          html = '<span>Something wrong, try again!</span>';          fail_popup.html(html);          window.setTimeout(function () {            fail_popup.fadeOut();            location.reload();          }, 1000);        } else if (data.status == 2) {          fail_popup.fadeIn();          fail_popup.addClass('of-popup-fail');          html = data.error;          fail_popup.html(html);          window.setTimeout(function () {            fail_popup.fadeOut();            location.reload();          }, 1000);        }      }    });  }});